  <li id="<%= dom_id scenario %>">
    <%= link_to scenario_path(scenario) do %>
      <%= "#{offtaker_signed_in? ? scenario.supplier_location.name : scenario.offtaker_location.name} - #{scenario.distance_pipeline}km" %> &rarr;
    <% end %>
    <button 
      data-action="click->maps#drawRoute" 
      data-location="<%= offtaker_signed_in? ? [scenario.supplier_location.longitude, scenario.supplier_location.latitude].to_json : [scenario.offtaker_location.longitude, scenario.offtaker_location.latitude].to_json %>"
      class="btn btn-primary"
      aria-label="Show on map" 
      data-balloon-pos="up"
    >
      <i class="fa-solid fa-route"></i>
    </button>
  </li>