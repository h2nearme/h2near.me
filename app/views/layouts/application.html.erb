<!DOCTYPE html>
<html>
  <head>
    <%= render partial: "layouts/head" %>
  </head>

  <body class="<%= "#{params[:controller]}-#{params[:action]}" %>">
    <%= render 'shared/navbar' %>
    <%= render 'shared/flashes' %>
    <main class="<%= 'devise' if params[:controller].include?('devise') %>">
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
  </body>
</html>
